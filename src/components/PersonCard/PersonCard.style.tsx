import {
    Box,
    useTheme

} from "@mui/material"
import styled from 'styled-components'

function usePersonCardStyles(theme: any) {

   


   return (
       styled.div`
           
       `
   )
}
export default usePersonCardStyles