import {
    Box,
    useTheme

} from "@mui/material"
import styled from 'styled-components'

function useLatenessSectionStyles(theme: any) {

   


   return (
       styled.div`
           
       `
   )
}
export default useLatenessSectionStyles