import {
    Box,
    useTheme

} from "@mui/material"
import styled from 'styled-components'

function useSitesSectionStyles(theme: any) {

   


   return (
       styled.div`
           
       `
   )
}
export default useSitesSectionStyles