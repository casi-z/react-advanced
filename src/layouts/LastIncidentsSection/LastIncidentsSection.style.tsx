import {
    Box,
    useTheme

} from "@mui/material"
import styled from 'styled-components'

function useLastIncidentsSectionStyles(theme: any) {

   


   return (
       styled.div`
           
       `
   )
}
export default useLastIncidentsSectionStyles