import {
    Box,
    useTheme

} from "@mui/material"
import styled from 'styled-components'

function useRadialBarSectionStyles(theme: any) {

   


   return (
       styled.div`
           
       `
   )
}
export default useRadialBarSectionStyles