import {
    Box,
    useTheme

} from "@mui/material"
import styled from 'styled-components'

function useStatisticSectionStyles(theme: any) {

   


   return (
       styled.div`
           
       `
   )
}
export default useStatisticSectionStyles