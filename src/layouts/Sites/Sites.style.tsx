import {
    Box,
    useTheme

} from "@mui/material"
import styled from 'styled-components'

function useSitesStyles(theme: any) {

   


   return (
       styled.div`
           
       `
   )
}
export default useSitesStyles