import {
    Box,
    useTheme

} from "@mui/material"
import styled from 'styled-components'

function useEventSectionStyles(theme: any) {

   


   return (
       styled.div`
           
       `
   )
}
export default useEventSectionStyles