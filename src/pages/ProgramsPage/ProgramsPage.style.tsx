import {
    Box,
    useTheme

} from "@mui/material"
import styled from 'styled-components'

function useProgramsPageStyles(theme: any) {

   


   return (
       styled.div`
           
       `
   )
}
export default useProgramsPageStyles